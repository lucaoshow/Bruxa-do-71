using UnityEngine;

namespace Root.Runes
{
    public class ChildRune : MergeableRune
    {
        public override void Activate(Vector2 playerLookingDir)
        {
            base.Activate(playerLookingDir);
        }
    }
}
